#include <stdio.h>

/**
 * main - Entry point
 *@argc: number returned
 *@argv: strings returned
 * Return: Always 0 (Success)
 */


int main(int argc, char *argv[])
{
argc = argc;

printf("%s\n", argv[0]);

return (0);
}
